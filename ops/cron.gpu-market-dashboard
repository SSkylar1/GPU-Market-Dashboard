# Run every 5 minutes against VAST live.
*/5 * * * * cd /Users/skylar/Dev/gpu-market-dashboard && /usr/bin/env zsh -lc '(INGEST_MODE=vast npm run collect && npm run recompute) >> /tmp/gpu-market-dashboard-cron.log 2>&1'
